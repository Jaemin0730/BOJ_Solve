#include <bits/stdc++.h>
using namespace std;
#define u ios::sync_with_stdio(false); cin.tie(NULL)
#define ll long long

int main(void)
{
    u;
    int n;
    cin >> n;
    if (n <= 10000) {
        cout << "Accepted";
    }
    else {
        cout << "Time limit exceeded";
    }
}