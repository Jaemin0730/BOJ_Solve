#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main(void)
{
    int n;
    cin >> n;
    if (n != 0) {
        cout << "1.00";
    }
    else {
        cout << "divide by zero";
    }
}