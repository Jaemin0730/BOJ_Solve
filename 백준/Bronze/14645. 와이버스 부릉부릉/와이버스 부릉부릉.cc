#include <stdio.h>
#pragma warning(disable : 4996)

/*
	

*/

int main() {
	int n, k;
	int a, b;
	scanf("%d %d", &n, &k);
	for (int i = 0; i < n; i++) {
		scanf("%d %d", &a, &b);
	}
	printf("비와이");
	return 0;
}