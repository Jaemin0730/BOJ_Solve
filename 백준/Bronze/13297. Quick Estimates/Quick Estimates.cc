#include <iostream>
#include <vector>
#include <algorithm>
#include <cstring>
using namespace std;


 
int main() {
    string a;
    int n;
    cin >> n;
    while (n--) {
        cin >> a;
        cout << a.size() << '\n';
    }
}