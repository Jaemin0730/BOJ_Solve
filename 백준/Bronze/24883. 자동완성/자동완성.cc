#include <bits/stdc++.h>
using namespace std;
#define u ios::sync_with_stdio(false); cin.tie(NULL)
#define ll long long

int main(void)
{
    u;
    char c;
    cin >> c;
    if (c == 'N' || c == 'n') {
        cout << "Naver D2";
    }
    else {
        cout << "Naver Whale";
    }
}