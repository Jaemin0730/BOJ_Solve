#include <iostream>
#include <algorithm>
#include <string>
#include <math.h>
#include <memory.h>
#include <stack>
#include <queue>
#include <vector>
#include <map>
#include <list>
#include <cmath>
#include <set>
using namespace std;
#define ll long long

void z()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
}

int main(void)
{
    z();
    int t;
    cin >> t;
    for (int i = 0; i < t; i++) {
        for (int j = 0; j < t; j++) {
            cout << "@@@@@";
        }
        cout << "\n";
    }
    //cout << "\n";
    for (int i = 0; i < t; i++) {
        for (int j = 0; j < t; j++) {
            cout << "@";
        }
        cout << "\n";
    }
    //cout << "\n";
    for (int i = 0; i < t; i++) {
        for (int j = 0; j < t; j++) {
            cout << "@@@@@";
        }
        cout << "\n";
    }
    //cout << "\n";
    for (int i = 0; i < t; i++) {
        for (int j = 0; j < t; j++) {
            cout << "@";
        }
        cout << "\n";
    }
    //cout << "\n";
    for (int i = 0; i < t; i++) {
        for (int j = 0; j < t; j++) {
            cout << "@@@@@";
        }
        cout << "\n";
    }
}