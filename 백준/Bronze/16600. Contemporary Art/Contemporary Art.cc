#include <bits/stdc++.h>
using namespace std;
#define u ios::sync_with_stdio(false); cin.tie(NULL)
#define ll long long

int main(void)
{
    u;
    double n;
    cin >> n;
    double a = 4 * sqrt(n);
    printf("%.7lf\n", a);
}