#include <stdio.h>
#pragma warning(disable : 4996)

int main() {
	int n;
	scanf("%d", &n);
	if (n == 1) {
		printf("Leading the Way to the Future\n");
	}
	else {
		printf("YONSEI\n");
	}

	return 0;
}