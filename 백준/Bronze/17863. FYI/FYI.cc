#include <bits/stdc++.h>
using namespace std;
#define u ios::sync_with_stdio(false); cin.tie(NULL)
#define ll long long

int main(void)
{
    u;
    string s;
    cin >> s;
    cout << (s.substr(0, 3) == "555" ? "YES" : "NO") << "\n";
}