#include <bits/stdc++.h>
using namespace std;
#define u ios::sync_with_stdio(false); cin.tie(NULL)
#define ll long long

int main(void)
{
    u;
    int n;
    cin >> n;
    if (n == 1) {
        cout << "SciComLove";
    }
    else {
        cout << "SciComLove\nSciComLove";
    }
}