#include <stdio.h>
#pragma warning(disable : 4996)

/*
	

*/

int main() {
	int a, b;
	scanf("%d %d", &a, &b);
	float result = 2 * a + 2 * b * 3.141592;
	printf("%f", result);
	return 0;
}